export function SectionTitle({ eyebrow, title, description, light = false }: { eyebrow: string; title: string; description?: string; light?: boolean }) { return <div className="max-w-3xl"><p className="eyebrow">{eyebrow}</p><h2 className={`display ${light ? "!text-white" : ""}`}>{title}</h2>{description && <p className={`mt-6 max-w-2xl text-lg leading-8 ${light ? "text-white/70" : "text-gray-600"}`}>{description}</p>}</div>; }
