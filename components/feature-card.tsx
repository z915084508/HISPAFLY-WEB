import type { ComponentType, SVGProps } from "react";
export function FeatureCard({ icon: Icon, title, text, dark = false }: { icon: ComponentType<SVGProps<SVGSVGElement>>; title: string; text: string; dark?: boolean }) { return <article className={`rounded-xl border p-7 transition duration-300 hover:-translate-y-1 ${dark ? "border-white/10 bg-white/[.04]" : "border-gray-200 bg-white shadow-soft"}`}><div className="mb-6 flex size-11 items-center justify-center rounded-lg bg-brand-yellow text-brand-navy"><Icon className="size-6" /></div><h3 className={`text-xl font-bold ${dark ? "text-white" : "text-brand-navy"}`}>{title}</h3><p className={`mt-3 leading-7 ${dark ? "text-white/65" : "text-gray-600"}`}>{text}</p></article>; }
